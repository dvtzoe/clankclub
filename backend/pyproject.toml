[project]
name = "backend"
version = "0.1.0"
description = "Backend for clackclub"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.127.0",
    "openai>=2.14.0",
    "python-dotenv>=1.2.1",
]
