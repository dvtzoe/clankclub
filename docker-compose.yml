services:
  backend:
    build: backend
    env_file:
      - backend/.env
    volumes:
      - ./backend:/app
    ports:
      - "8000:8000"
    command: uv run fastapi run src/main.py --reload --host 0.0.0.0
